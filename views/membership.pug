extends layout

block content
    h1 #{title} Page
    
    if !user.membership
        p Enter secret "SECRET CODE" to join the club.
    
        form(method="POST" action="/membership")

            input(type="text" name="code" required)
            input(type="submit" value="Submit")
    else 
        p You are a member already.


